apiVersion: v1
kind: Pod
metadata:
  labels:
    jenkins: kubepod-slave
spec:
  containers:
  - name: docker-container
    image: docker:latest
    command:
      - dockerd
    tty: true
    privileged: true